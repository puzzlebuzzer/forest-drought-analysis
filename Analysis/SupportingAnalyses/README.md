# Supporting Analyses

Scripts in this directory produce non-dashboard analytical outputs such as report figures, diagnostics, anomaly products, slope/aspect investigations, ArcGIS exports, and exploratory ecozone analyses.

These scripts may use the same cached rasters, trait layers, or summary tables as the dashboard, but they do not create the core `SummaryTables/dashboard_data` products that power the dashboard.
