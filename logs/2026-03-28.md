# Session Log — 2026-03-28

## Time

- 2026-03-28 19:56:04 PDT

## Goal

- Re-establish a trustworthy project baseline
- Clarify which cache state is authoritative
- Decide what to delete to recover disk space

## Source-of-Truth Context

- `docs/PROJECT_OVERVIEW.md` is the primary source of truth for current project state

## What Was Verified

- The repo root is `Python/`, while project data lives under `../AOI/`
- `_3_4` Sentinel caches exist for both north and south and are currently usable
- `_3_24` north contains a partial Sentinel rebuild
- `_3_24` south is effectively empty
- `_3_4` ecozone and terrain trait rasters exist for both AOIs
- Landsat data also exists under `_3_4`
- `Results_cache1/` contains the historical validated figures and layer packages from `_3_4`
- `Results/` is effectively a future rerun destination and is not the validated archive
- The core script set appears to descend from the `_3_4` working pipeline, even where files were later renamed or generalized
- Trust concerns are strongest around later `_3_24`-era edits and one-off support scripts, not around the entire script lineage

## Cache Snapshot

- `GWNF_cache_3_24`: about 41G, partial north Sentinel rebuild
- `Smoky_cache_3_24`: effectively empty
- `GWNF_cache_3_4`: about 181G total
- `Smoky_cache_3_4`: about 228G total

Key interpretation:

- `_3_4` is the stable analysis base
- `_3_24` is not trusted enough to preserve during stabilization
- `Results_cache1/` is the last known-good deliverable archive

## Decision

The `_3_24` caches are being abandoned and deleted.

Reason:

- disk space is full
- the partial rebuild is not trusted enough to justify preserving it
- stabilization is more important than continuing the rebuild
- updated data with broader SCL handling remains a future goal

## Lessons Learned

- The main trust breakdown was in the `_3_24` rebuild process, especially around rate-limit handling, signed-token refresh, and restart behavior
- Another major failure was an incorrect assumption that `_3_4` had already been deleted, which obscured true disk usage and contributed to the full-disk stop
- A large part of the difficulty has been project-state management: keeping the technical state, AI-assisted changes, and collaborator-facing documentation aligned
- This is the first large project in this workspace to rely heavily on AI agents, so part of the stabilization effort is also establishing a better operating model for future sessions

## Why Documentation Is Now A Priority

- project state has become too difficult to manage reliably in memory alone
- collaborators need a clearer orientation to what is stable, what is experimental, and what has actually been run
- process documentation is now part of the technical stabilization strategy, not just an administrative add-on

## Operational Rule Going Forward

- Treat `_3_4` as the only authoritative cache baseline until this log and `PROJECT_OVERVIEW.md` say otherwise
- Treat `Results_cache1/` as the authoritative validated output archive until a future rerun is explicitly validated
- Treat one-off repair and support scripts separately from the historical core pipeline when judging trust

## Next Safe Step

- Manually delete `AOI/NorthAOI/GWNF_cache_3_24`
- Manually delete `AOI/SouthAOI/Smoky_cache_3_24`
- After deletion, verify freed space and document the trust boundary between `_3_4`, `Results_cache1/`, and the unvalidated `_3_24` rerun path
